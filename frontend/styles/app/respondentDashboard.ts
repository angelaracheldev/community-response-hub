import { StyleSheet } from 'react-native';

export const respondentDashboardStyles = StyleSheet.create({
  loading: {
    paddingVertical: 48,
    alignItems: 'center',
  },
  section: {
    marginTop: 12,
  },
});
