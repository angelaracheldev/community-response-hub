# Backend API (no trailing slash).
# Production (Railway):
EXPO_PUBLIC_API_URL=https://community-response-hub-production.up.railway.app

# Local dev: comment out the line above or delete .env to use http://localhost:5000
