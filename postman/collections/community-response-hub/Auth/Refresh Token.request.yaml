$kind: http-request
name: Refresh Token
method: POST
url: '{{baseUrl}}/api/v1/auth/refresh-token'
order: 5000
auth:
  type: noauth
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "refreshToken": "{{refreshToken}}"
    }
